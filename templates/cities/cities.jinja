{% extends 'base.jinja'%}

{% block content %}
<h2> <b> <u> <em></em>City List </em> </u> </b> </h2>

</select>
<div>


    {# <form action="/cities/delete/{{city.id}}" method="post"></form>
    <label for="cities">City:</label> #}
   <form action="/cities" method="post"> <input type="text" id="cities" name="name" placeholder="Add New City" required autofocus>
    <button type="submit">Add City</button>
    </form> 
    </select>
</div>


<div class="city"></div>
{%for city in cities%}

<h4>{{ city.name}} </h4>
<form action="/cities/delete/{{city.id}}" method="post">
    <input type="submit" value="Delete City" />
    {%endfor%}

    {% endblock %}